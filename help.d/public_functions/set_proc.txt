
Name:
	set_proc

Description:
	Set a configuration value in the `/proc' filesystem.
	Add the command to the command queue.

Usage:
	set_proc value path message

Example:
	set_proc 1 '/proc/sys/net/ipv4/conf/all/log_martians' 'Enabling logging of martians on all interfaces'
